import React from 'react';
import type { ResetPassScreen } from 'mobile/src/navigations/AuthStack';

/**
 * Stub screen for when reset password flow becomes integrated into mobile.
 * Currently this is completed on desktop.
 */
const ResetPass: ResetPassScreen = () => {
  return <></>;
};

export default ResetPass;
