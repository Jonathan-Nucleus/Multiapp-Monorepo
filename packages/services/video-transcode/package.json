{
  "name": "video-transcode",
  "version": "1.0.0",
  "description": "Microservice handling AWS MediaConvert to db flow.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongodb": "^4.6.0",
    "morgan": "^1.10.0",
    "request": "^2.88.2"
  }
}
